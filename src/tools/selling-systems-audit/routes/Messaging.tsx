/**
 * Messaging & Positioning — Phase 0 placeholder.
 * Real shell + fields land in later phases.
 */
export function Messaging() {
  return (
    <div className="app-content py-16 flex flex-col gap-3">
      <h1 className="text-2xl" style={{ letterSpacing: "-0.02em" }}>
        Messaging & Positioning
      </h1>
      <p className="text-ink-muted text-sm max-w-prose">
        Coming together — the intake for this section will appear here shortly.
      </p>
    </div>
  );
}
